name: Railway Service Scheduler

on:
  workflow_dispatch:   # permite rodar manualmente
  schedule:
    # Ativa às 6h de segunda a sexta
    - cron: "0 6 * * 1-5"
    # Pausa às 18h de segunda a sexta
    - cron: "0 18 * * 1-5"

jobs:
  railway-service:
    runs-on: ubuntu-latest
    steps:
      - name: Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Instalar Railway CLI
        run: npm install -g @railway/cli

      - name: Autenticar Railway
        run: railway login --token ${{ secrets.RAILWAY_TOKEN }}

      - name: Resume Service (escala para 1 réplica)
        if: github.event.schedule == '0 6 * * 1-5'
        run: railway scale 091b247d-e42f-488d-a5ef-de49f997dd1d 1

      - name: Pause Service (escala para 0 réplicas)
        if: github.event.schedule == '0 18 * * 1-5'
        run: railway scale 091b247d-e42f-488d-a5ef-de49f997dd1d 0
