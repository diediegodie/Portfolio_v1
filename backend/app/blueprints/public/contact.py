"""Deprecated contact blueprint.

The contact view is now rendered in a modal on the homepage, so this
blueprint is no longer registered. Keep the file as a placeholder in
case the contact modal needs to be refactored back into a standalone route.
"""
